"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const events = require("events");
const request = require("request");
const accept_ranges_1 = require("./accept-ranges");
class PartialDownload extends events.EventEmitter {
    start(url, range, headers) {
        const options = {};
        options.headers = headers || {};
        options.headers.Range = `${accept_ranges_1.AcceptRanges.Bytes}=${range.start}-${range.end}`;
        let offset = range.start;
        request
            .get(url, options)
            .on('error', (err) => {
            this.emit('error', err);
        })
            .on('data', (data) => {
            this.emit('data', data, offset);
            offset += data.length;
        })
            .on('end', () => {
            this.emit('end');
        });
        return this;
    }
}
exports.PartialDownload = PartialDownload;
